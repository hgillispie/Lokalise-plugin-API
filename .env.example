# ============================================================================
# LOKALISE API CONFIGURATION - DEVELOPMENT/TESTING ONLY
# ============================================================================
# IMPORTANT: These values are FALLBACKS for development and testing.
# In production, the Builder.io plugin provides authentication values:
# - Users enter their API token in the plugin settings UI
# - Users select projects dynamically in the plugin interface
# - Plugin sends these values with each API request
#
# Only set these for local development without the plugin:
LOKALISE_API_TOKEN=
LOKALISE_PROJECT_ID=

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated URLs)
ALLOWED_ORIGINS=http://localhost:1234,https://builder.io,https://app.builder.io

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Database URL for caching/sessions (future)
# DATABASE_URL=postgresql://user:pass@host:port/db
